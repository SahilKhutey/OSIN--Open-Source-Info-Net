apiVersion: apps/v1
kind: Deployment
metadata:
  name: osin-audio-intel
  namespace: osin
  labels:
    app: osin-audio-intel
    version: 2.1.0
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  selector:
    matchLabels:
      app: osin-audio-intel
  template:
    metadata:
      labels:
        app: osin-audio-intel
        version: 2.1.0
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8012"
        prometheus.io/path: "/metrics"
    spec:
      containers:
      - name: audio-intel
        image: osin-audio-intel:2.1.0
        ports:
        - containerPort: 8012
          name: http
        resources:
          requests:
            memory: "1Gi"
            cpu: "500m"
          limits:
            memory: "3Gi"
            cpu: "1000m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8012
          initialDelaySeconds: 45
          periodSeconds: 20
        readinessProbe:
          httpGet:
            path: /health
            port: 8012
          initialDelaySeconds: 15
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: osin-audio-intel
  namespace: osin
spec:
  selector:
    app: osin-audio-intel
  ports:
  - port: 8012
    targetPort: 8012
  type: ClusterIP

apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: youtube-ingestion-monitor
  namespace: monitoring
spec:
  selector:
    matchLabels:
      app: youtube-ingestion
  endpoints:
  - port: metrics
    interval: 30s
    path: /metrics

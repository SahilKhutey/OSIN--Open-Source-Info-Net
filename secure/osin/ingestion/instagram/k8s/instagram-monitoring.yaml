apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: instagram-ingestion-monitor
  namespace: monitoring
spec:
  selector:
    matchLabels:
      app: instagram-ingestion
  endpoints:
  - port: metrics
    interval: 30s
    path: /metrics

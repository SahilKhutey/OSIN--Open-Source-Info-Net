apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: twitter-ingestion-monitor
  namespace: monitoring
spec:
  selector:
    matchLabels:
      app: twitter-ingestion
  endpoints:
  - port: metrics
    interval: 30s
    path: /metrics
